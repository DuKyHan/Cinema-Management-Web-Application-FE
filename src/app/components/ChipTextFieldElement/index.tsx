import { Autocomplete, Chip, TextField, TextFieldProps } from '@mui/material';

// export const ChipTextFieldElement = (
//   props: UseControllerProps<TextFieldProps>,
// ) => {
//   return (
//     <Autocomplete
//       freeSolo
//       multiple
//       options={[]}
//       renderTags={(value, props) =>
//         value.map((option, index) => (
//           <Chip label={option} {...props({ index })} />
//         ))
//       }
//       renderInput={params => <TextField label="Add Tags" {...params} />}
//     />
//   );
// };
